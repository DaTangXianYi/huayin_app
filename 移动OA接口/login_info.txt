*** Settings ***
Library           RequestsLibrary
Library           Collections

*** Test Cases ***
登录接口
    Create Session    api    http://192.168.25.16:8099
    ${addr}    get request    api    hyoa/camel/mapi/mobileRestWebService/login_info?password=abcd1234&login_name=guobin&papandid=123
    Should Be Equal As Integers    ${addr.status_code}    200
    log    ${addr.content}
    ${responsedata}    to json    ${addr.content}
    ${keys}    Get Dictionary Keys    ${responsedata}
    ${items}    Get Dictionary items    ${responsedata}
    ${values}    Get Dictionary values    ${responsedata}
    ${id}    get from dictionary    ${responsedata}    id
    ${loginName}    get from dictionary    ${responsedata}    loginName
    ${status}    get from dictionary    ${responsedata}    status
    ${description}    get from dictionary    ${responsedata}    description
    Run Keyword if    ${status}==1    log    登录成功
    Delete All Sessions
